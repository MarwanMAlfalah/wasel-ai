# Convex Base Setup

This folder is reserved for Convex functions and schema files.

The app is already wired with a build-safe `ConvexClientProvider` that activates
only when `NEXT_PUBLIC_CONVEX_URL` is present.

Schema, queries, mutations, and actions will be added in the next implementation
step.
